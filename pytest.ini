[pytest]

addopts =
    -vv
    --cov-config=.coveragerc
    --cov-report html:coverage/html
    --cov-report xml:coverage/coverage.xml
    --cov-report term
    --cov=.
testpaths = fretboardgtr/tests
filterwarnings =
    ignore::DeprecationWarning
    default:::fretboardgtr.*
